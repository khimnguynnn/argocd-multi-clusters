apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis-prd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/khimnguynnn/argocd-multi-clusters.git
    targetRevision: main
    path: kustomize/redis/overlays/prd
  destination:
    server: https://kubernetes.default.svc
    namespace: redis
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true